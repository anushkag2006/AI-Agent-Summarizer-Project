# requirements.txt for AI Agent: Automatic Research Paper Reader and Summarizer
# This project uses Hugging Face Transformers for summarization, PyMuPDF for PDF extraction,
# and basic NLP tools. Assumes Python 3.8+.

# Core dependencies
transformers>=4.30.0
torch>=2.0.0  # For running models (CPU/GPU)

# PDF reading
PyMuPDF>=1.22.0  # Fast PDF text extraction

# Text processing
nltk>=3.8.0
spacy>=3.5.0

# Additional utilities
sentencepiece>=0.1.99  # For some tokenizer models
requests>=2.28.0  # For downloading models if needed
tqdm>=4.65.0  # Progress bars

# Optional: For advanced agent features (e.g., chaining with LangChain)
# langchain>=0.0.300
# openai>=0.28.0  # If using GPT for summarization
